<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:key="@string/pref_sort_by_key"
        android:title="@string/pref_sort_by_label"
        android:defaultValue="@string/pref_sort_by_default"
        android:summary="%s"
        android:entries="@array/pref_sort_by_list_labels"
        android:entryValues="@array/pref_sort_by_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />
</PreferenceScreen>
